<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.microservices.user.biz.mapper.PermissionMapper">

    <resultMap id="permissionPoMap" type="org.microservices.user.biz.po.PermissionPo">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="name" property="name"/>
        <result column="method" property="method"/>
        <result column="path" property="path"/>
        <result column="desc" property="desc"/>
        <result column="additional_info" property="additionalInfo"/>
        <result column="del_flag" property="delFlag"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryForListByRoleId" resultMap="permissionPoMap">
        <!-- ### -->
        SELECT
        p.id,
        p.parent_id,
        p.`name`,
        p.`method`,
        p.`path`,
        p.`desc`,
        p.additional_info,
        p.del_flag,
        p.create_time,
        p.update_time
        FROM permission p
        JOIN role_permission rp ON rp.permission_id = p.id
        WHERE rp.role_id = #{roleId}
    </select>

</mapper>
