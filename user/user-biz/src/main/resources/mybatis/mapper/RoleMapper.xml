<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.microservices.user.biz.mapper.RoleMapper">

    <resultMap id="rolePoMap" type="org.microservices.user.biz.po.RolePo">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="desc" property="desc"/>
        <result column="additional_info" property="additionalInfo"/>
        <result column="del_flag" property="delFlag"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="rolePoMap" resultMap="rolePoMap">
        <!-- ### -->
        SELECT
        r.id,
        r.`name` AS 'name',
        r.`code` AS 'code',
        r.`desc` AS 'desc',
        r.additional_info,
        r.del_flag,
        r.create_time,
        r.update_time
        FROM `role` r
        JOIN user_role ur ON ur.role_id = r.id
        WHERE
        ur.user_id = #{userId}
    </select>

</mapper>
